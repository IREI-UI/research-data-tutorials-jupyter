[project]
name = "research-data-tutorials"
version = "0.1.0"
description = "Notebooks for working with research data using DuckDB and Parquet"
requires-python = ">=3.13"

dependencies = [
    "duckdb>=1.0",
    "pandas>=2.0",
    "pyarrow>=14.0",
    "jupyterlab>=4.0",
    "jupyter-matlab-proxy>=0.19.1",
]

[project.optional-dependencies]
dev = [
    "ipykernel",
]

[dependency-groups]
dev = [
    "ipykernel",
    "nbstripout>=0.9.1",
]
